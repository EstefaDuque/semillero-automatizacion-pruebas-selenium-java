package com.instagram.certification.test;

//Importación libreria Assert
import static org.junit.Assert.assertNotNull; //Para el método assertNotNull.
import static org.junit.Assert.assertNull;//Para el método assertNull.
//-----------------------------------
import org.openqa.selenium.WebElement;

/*Clase Assert: JUnit proporciona métodos estáticos en la clase Assert para probar ciertas
condiciones. 
Estos métodos de afirmación típicamente comienzan con assert y le permiten especificar 
el mensaje de error, el esperado y el resultado real. 
Un método afirmación compara el valor real devuelto por una prueba para el valor esperado,
y se produce una AssertionException si la prueba de comparación falla.*/

public class TestValidationElement {

	public TestValidationElement() {
	}
	
	public static void validarEncuentroBoton(WebElement elemento){
	
        /*El método assertNull Afirma que un objeto es nulo. 
        Si no lo es, AssertionError se lanza un mensaje con el mensaje dado.*/
		assertNull("Se encontro el elemento en la página",elemento);
		
		/*El método assertNotNull Afirma que un objeto no es nulo.
		Si no lo es, AssertionError se lanza un mensaje con el mensaje dado.*/
	    assertNotNull("No se encontro el elemento en la página",elemento);
	}

}
